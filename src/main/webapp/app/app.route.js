// 需要url跳转时启用
/*(function() {
    'use strict';

    angular
        .module('bsbmsoneApp')
        .config(routeConfig);

    routeConfig.$inject = ['$urlRouterProvider'];

    function routeConfig($urlRouterProvider) {
        $urlRouterProvider.when('/', '/csgo');
    }
})();*/
